name: rido
description: "RIDO , Mobile platform connecting riders with drivers for on-demand transportation, letting users book rides, track arrivals, pay digitally"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Core Architecture
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  go_router: ^14.0.0
  fquery: ^1.2.0
  sizer: ^2.0.15 # MANDATORY

  # UI Ecosystem (The Quad-Stack)
  shadcn_ui: 
  getwidget: 
  shadcn_flutter: any 
  forui: any
  
  # Animations & Interactions
  lottie:
  flutter_svg: ^2.0.10
  animations: 
  page_transition: 
  motion: 
  flutter_map_animations: 
  haptic_feedback: 

  # Maps & Navigation
  # Maps & Navigation
  google_maps_flutter: ^2.5.0 
  geolocator: ^13.0.0 
  # navigation_map: ^0.0.1 # Warning: These might be obscure, checking availability via pub get
  # nb_navigation_flutter: ^0.0.1
  # google_maps_widget: ^0.0.1
  
  # Utilities
  logger: 
  talker: 
  talker_flutter: 
  internet_connection_checker_plus: 
  permission_handler: 
  keyboard_safe: 
  pinput: 
  awesome_snackbar_content: 
  toggle_switch: 
  smart_tooltip: 
  multilingual: 
  intl: 
  introduction_screen: 
  image_picker:
  
  # Backend (Firebase)
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  cloud_functions: ^5.0.0
  firebase_storage: ^12.0.0
  firebase_messaging: ^15.0.0
  firebase_crashlytics: ^4.0.0
  firebase_analytics: ^11.0.0
  firebase_performance: ^0.10.0
  firebase_app_check: ^0.3.0
  
  # AI
  google_generative_ai: ^0.2.0

  # Assets & Fonts
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.6
  
  # Generation
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  shared_preferences: ^2.5.4
  dotted_border: ^3.1.0
  flutter_dotenv: ^6.0.0
  http: ^1.6.0
  uuid: ^4.5.2
  geocoding: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.8
  freezed: ^2.4.7
  json_serializable: ^6.7.1
  riverpod_generator: ^2.4.0
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.3.10



flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/lottie/
    - assets/images/
    - assets/
